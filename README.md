# GetLatestVerTool

目前在模块目录下有多个程序员开发的模块。打包安装程序时需要获取每个模块的最新版本，手动获取麻烦，因此需要有一个自动获得所有模块最新版本的工具。

模块目录下为两层目录结构:

第一层 模块名称， 如【系统备份】

第二层 模块版本，前8个字符固定为时间，最近时间的就是最新版本，这一层目录下的所有文件与子目录都视为这个模块的文件。

设计文档见doc/模版获取工具任务设计书.doc
